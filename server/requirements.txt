# VulnFix Agent — maven-mcp-server dependencies
# Install with: pip install -r requirements.txt

# MCP server framework
fastmcp>=2.0.0

# HTTP client for Nexus/Artifactory REST API calls (async-capable)
httpx>=0.27.0

# YAML config file parsing
pyyaml>=6.0.1
